import styled from 'styled-components'

export default function Header() {
  return (
    <StyledHeader>
      Header
    </StyledHeader>
  )
}

const StyledHeader = styled.header`
  grid-area: 1 / 1 / 2 / 2;
`
